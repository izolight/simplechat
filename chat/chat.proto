syntax = "proto3";

package chat;

service Chat {
    rpc SendMessage (stream UserMessage) returns (stream ServerMessage) {}
}

message UserMessage {
    string User = 1;
    string Message = 2;
}

message ServerMessage {
    string Message = 1;
}